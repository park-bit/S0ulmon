# ============================================================
# solar-aggregator — Python dependencies
# Python 3.12+
# ============================================================

# HTTP client
requests==2.32.3

# Environment variable loading
python-dotenv==1.0.1

# Data validation and modelling
pydantic==2.7.4
pydantic-settings==2.3.4

# Retry / resilience
tenacity==8.3.0

# Structured logging
loguru==0.7.2

# Periodic scheduling
schedule==1.2.2

# SHA1 / HMAC utilities (stdlib, listed for explicitness — no install needed)
# hashlib  — stdlib
# hmac     — stdlib
# time     — stdlib

# Database and Authentication
pymongo[srv]>=4.6.3
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
